class Models {
    write(guildId) {
        // TODO: save in the PSQL database
    }

    search(guild, table, domain) {
        // TODO: search in the PSQL database
    }
}
export const models = new Models();
