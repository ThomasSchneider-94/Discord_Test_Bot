# X Embedding

Module that provides a proper embedding for X (formerly Twitter) links automatically.

## Features

When a message contains a link to an X post, the bot will automatically remove the preview and re-send it with a proper embedding of the X post. A message modification or deletion will also trigger the same process on the bot's response.
